"""agent-cron-ops — universal cron job health checking for any agent.

Subpackages:
  cron_ops.analyzer — error pattern knowledge base
  cron_ops.alerts   — alert delivery (Feishu + generic webhook)
  cron_ops.cli      — check / check-all / analyze commands
  cron_ops.wrap     — command wrapper entry point
"""

__version__ = "1.0.0"
